package com.bpdevop.mediccontrol.ui.screens

import androidx.compose.runtime.Composable

@Composable
fun VaccinationHistoryScreen() {
    /*// Aquí recibirás el historial de las vacunas del paciente
    val vaccineHistory = emptyList()

    LazyColumn(modifier = Modifier.fillMaxSize()) {
        items(vaccineHistory) { vaccine ->
            //VaccineHistoryItem(vaccine)
        }
    }*/
}

/*@Composable
fun VaccineHistoryItem(vaccine: Vaccine) {
    Column(modifier = Modifier.padding(16.dp)) {
        Text(text = vaccine.name, style = MaterialTheme.typography.h6)
        Text(text = vaccine.date, style = MaterialTheme.typography.body2)
        Text(text = vaccine.notes, style = MaterialTheme.typography.body2)
    }
}*/
